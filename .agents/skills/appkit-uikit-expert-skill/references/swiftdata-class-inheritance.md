# SwiftData Class Inheritance

Use this reference when modeling SwiftData entities with inheritance.

## Priority
- If this reference conflicts with local repo guidance or `AGENTS.md`, follow those.

## Guidance
- Keep model definitions in Core; avoid UI dependencies.
- Plan queries and predicates carefully for subclass types.
- Account for schema evolution and migrations when introducing inheritance.

## References
- Read `references/SwiftData-Class-Inheritance.md` for modeling and query details.
